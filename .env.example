# ============================================================================
# EVA-VOICE v1.1.0 - Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Groq API Key (for LLM responses)
# ----------------------------------------------------------------------------
# Get your free API key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_groq_api_key_here

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
EVA_API_KEY=eva-prod-key-change-this
EVA_DEV_MODE=true

# Rate Limiting
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=60

# Database (optional, defaults to local file)
DB_PATH=eva_conversations.db

# ----------------------------------------------------------------------------
# Frontend Configuration
# ----------------------------------------------------------------------------
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# ============================================================================
# INSTALLATION GUIDE
# ============================================================================
#
# Prerequisites:
#   - Node.js 18+ (https://nodejs.org/)
#   - Python 3.10+ (https://python.org/)
#   - Bun (recommended): curl -fsSL https://bun.sh/install | bash
#
# ----------------------------------------------------------------------------
# 1. BACKEND SETUP
# ----------------------------------------------------------------------------
#
#   cd backend
#   python -m venv venv
#   source venv/bin/activate        # Linux/Mac
#   # venv\Scripts\activate         # Windows
#   pip install -r requirements.txt
#
#   # Start backend:
#   python -m uvicorn main:app --host 0.0.0.0 --port 8000
#
# ----------------------------------------------------------------------------
# 2. FRONTEND SETUP
# ----------------------------------------------------------------------------
#
#   cd frontend
#   bun install                     # or: npm install
#   bun dev --port 3001             # or: npm run dev -- --port 3001
#
#   Open: http://localhost:3001
#
# ----------------------------------------------------------------------------
# 3. SADTALKER SETUP (Optional - for real lip sync)
# ----------------------------------------------------------------------------
#
#   cd sadtalker
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#   pip install fastapi uvicorn python-multipart httpx
#
#   # Download models (run once):
#   bash scripts/download_models.sh
#
#   # Start SadTalker API:
#   python api.py
#
#   Note: SadTalker requires ~4GB disk space for models
#         Generation is slow on CPU (~4min), fast on GPU (~20s)
#
# ----------------------------------------------------------------------------
# 4. AVAILABLE PAGES
# ----------------------------------------------------------------------------
#
#   http://localhost:3001/           - Main chat interface
#   http://localhost:3001/call       - Voice call with Eva
#   http://localhost:3001/avatar-transparent - Real lip sync (requires SadTalker)
#   http://localhost:3001/facetime   - Facial emotion detection (camera)
#   http://localhost:3001/voicemotion - Voice emotion analysis (microphone)
#
# ----------------------------------------------------------------------------
# 5. PORTS USED
# ----------------------------------------------------------------------------
#
#   3001 - Frontend (Next.js)
#   8000 - Backend API (FastAPI)
#   8003 - SadTalker API (optional)
#
# ----------------------------------------------------------------------------
# 6. TROUBLESHOOTING
# ----------------------------------------------------------------------------
#
#   "GROQ_API_KEY not set":
#     -> Create .env file with your Groq API key
#
#   "Cannot connect to backend":
#     -> Make sure backend is running on port 8000
#
#   "SadTalker API offline":
#     -> Start SadTalker: cd sadtalker && source venv/bin/activate && python api.py
#
#   "numpy.float error":
#     -> Already fixed in this version, just pull latest code
#
#   Slow lip sync generation:
#     -> Normal on CPU (~4min). Use GPU for faster results (~20s)
#
# ============================================================================
